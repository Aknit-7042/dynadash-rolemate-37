
// This file is converted from TypeScript to JavaScript
// Original type definitions are kept as comments for reference

/*
export type Role = 'hr' | 'manager' | 'employee';

export interface User {
  id: string;
  name: string;
  email: string;
  avatar?: string;
  roles: Role[];
  primaryRole: Role;
}

export interface NavigationItem {
  name: string;
  href: string;
  icon: React.ComponentType<{ className?: string }>;
  roles: Role[];
}

export interface Task {
  id: string;
  title: string;
  description: string;
  status: 'pending' | 'in-progress' | 'completed';
  dueDate: string;
  assignedTo: string;
}

export interface LeaveRequest {
  id: string;
  employeeId: string;
  employeeName: string;
  startDate: string;
  endDate: string;
  reason: string;
  status: 'pending' | 'approved' | 'rejected';
}

export interface Expense {
  id: string;
  employeeId: string;
  employeeName: string;
  amount: number;
  category: string;
  date: string;
  status: 'pending' | 'approved' | 'rejected';
}

export interface AttendanceRecord {
  id: string;
  employeeId: string;
  date: string;
  checkin: string;
  checkout: string | null;
  status: 'present' | 'absent' | 'late' | 'half-day';
}

export interface SalarySlip {
  id: string;
  employeeId: string;
  month: string;
  year: number;
  basicSalary: number;
  allowances: number;
  deductions: number;
  netSalary: number;
  issueDate: string;
}

export interface CompanyUpdate {
  id: string;
  title: string;
  content: string;
  date: string;
  author: string;
  important: boolean;
}

export interface Employee {
  id: string;
  name: string;
  email: string;
  position: string;
  department: string;
  joinDate: string;
  managerId: string | null;
}

export interface PerformanceMetric {
  id: string;
  employeeId: string;
  category: string;
  score: number;
  period: string;
  comments: string;
}
*/

// The JavaScript version doesn't need to export types
// The objects will use the structure defined in the TypeScript version
